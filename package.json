{
  "name": "a-occhio-party-game",
  "version": "2.2.0",
  "private": true,
  "description": "A OCCHIO! - party game multiplayer con master, timer server-side e modalit\u00e0 risposta a voce",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node scripts/check-game.js",
    "test": "node --test tests/*.test.js",
    "audit:questions": "node scripts/audit-questions.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
